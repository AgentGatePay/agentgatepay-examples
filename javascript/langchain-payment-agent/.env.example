# =============================================================================
# AgentGatePay + LangChain.js - Environment Configuration
# =============================================================================
# IMPORTANT: Copy this file to .env and replace placeholder values
# Command: cp .env.example .env && nano .env
# =============================================================================

# -----------------------------------------------------------------------------
# 1. AgentGatePay API Configuration
# -----------------------------------------------------------------------------
# Get API keys: curl -X POST https://api.agentgatepay.com/v1/users/signup
# Create 2 accounts (buyer + seller) for marketplace examples
# -----------------------------------------------------------------------------
AGENTPAY_API_URL=https://api.agentgatepay.com
MCP_API_URL=https://mcp.agentgatepay.com
BUYER_API_KEY=pk_live_YOUR_BUYER_KEY        # From signup response (shown once)
BUYER_EMAIL=your-buyer-email@example.com    # Your buyer account email
SELLER_API_KEY=pk_live_YOUR_SELLER_KEY      # From signup response (shown once)
SELLER_EMAIL=your-seller-email@example.com  # Your seller account email

# -----------------------------------------------------------------------------
# 2. Multi-Chain Payment Configuration
# -----------------------------------------------------------------------------
# PAYMENT_CHAIN: base (recommended), ethereum, polygon, arbitrum
# PAYMENT_TOKEN: USDC, USDT (Ethereum only), DAI
# Base has lowest fees (~$0.001 per TX) and fastest settlement (2-5 sec)
# -----------------------------------------------------------------------------
PAYMENT_CHAIN=base          # Recommended: base (low fees, fast)
PAYMENT_TOKEN=USDC          # USDC available on all chains

# -----------------------------------------------------------------------------
# 3. Blockchain RPC Endpoints
# -----------------------------------------------------------------------------
# Free RPCs work for testing. Premium RPCs (Alchemy/Infura) are faster
# For Ethereum: Use premium RPC for 10-20x faster payment verification
# Get free Alchemy key: https://www.alchemy.com/ (300M requests/month free)
# -----------------------------------------------------------------------------
BASE_RPC_URL=https://mainnet.base.org
ETHEREUM_RPC_URL=https://eth-mainnet.public.blastapi.io
# Premium Ethereum (faster): https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY
POLYGON_RPC_URL=https://polygon-rpc.com
ARBITRUM_RPC_URL=https://arb1.arbitrum.io/rpc

# -----------------------------------------------------------------------------
# 4. Wallet Configuration (SECURITY: Keep private keys secure!)
# -----------------------------------------------------------------------------
# BUYER_PRIVATE_KEY: 64-character hex (from MetaMask or ethers.js)
# BUYER_WALLET: Buyer's public address (0x...)
# SELLER_WALLET: Seller's public address (0x...)
#
# Generate wallet (Node.js):
#   const { ethers } = require('ethers');
#   const wallet = ethers.Wallet.createRandom();
#   console.log('Address:', wallet.address);
#   console.log('Private Key:', wallet.privateKey);
# -----------------------------------------------------------------------------
BUYER_PRIVATE_KEY=0xYOUR_64_CHAR_PRIVATE_KEY_HERE  # DO NOT COMMIT THIS!
BUYER_WALLET=0xYOUR_BUYER_WALLET_ADDRESS
SELLER_WALLET=0xYOUR_SELLER_WALLET_ADDRESS

# -----------------------------------------------------------------------------
# 5. LLM API Key (for LangChain agent intelligence)
# -----------------------------------------------------------------------------
# OpenAI (default): Get key from https://platform.openai.com/api-keys
# Anthropic Claude: Swap OPENAI_API_KEY → ANTHROPIC_API_KEY in code
# Google Gemini: Use GOOGLE_API_KEY in code
# Local models: Use Ollama/LM Studio (no key needed)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-YOUR_OPENAI_API_KEY_HERE

# -----------------------------------------------------------------------------
# 6. External TX Signing Service (OPTIONAL - for production)
# -----------------------------------------------------------------------------
# Deploy: docker run -d -p 3000:3000 agentgatepay/tx-signing-service:latest
# Or use Render: https://render.com/deploy?repo=https://github.com/AgentGatePay/TX
# Examples 1b, 3b use this service (other examples use local signing)
# -----------------------------------------------------------------------------
TX_SIGNING_SERVICE=https://your-service.onrender.com  # Optional

# -----------------------------------------------------------------------------
# 7. Marketplace Configuration (for Examples 2a/2b, 4a/4b)
# -----------------------------------------------------------------------------
# SELLER_API_URL: Express API URL (usually localhost:8000)
# MANDATE_BUDGET_USD: Buyer's payment budget (default: $100)
# -----------------------------------------------------------------------------
SELLER_API_URL=http://localhost:8000  # Seller API endpoint
MANDATE_BUDGET_USD=100.0              # Buyer mandate budget

# =============================================================================
# Quick Start Checklist
# =============================================================================
# ✅ Replace all YOUR_* placeholders above
# ✅ Fund buyer wallet with USDC (~$10) + gas ETH (~$0.01)
# ✅ Run: npm install
# ✅ Test: npm run example:1a
# =============================================================================
